"""tests — stdlib unittest suite for rollout-shield.

The suite ships zero third-party deps: it uses only ``unittest`` from the
standard library so it runs on any Python 3.8+ without setup. Run it via:

    python -m unittest discover -s tests -v
    # or the convenience entry point:
    python tests/run_all.py
"""
