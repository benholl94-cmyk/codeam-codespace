"""Subcommand implementations for the rollout-shield CLI.

Each module exports a top-level function named ``cmd_<verb>`` that
takes a ``State`` and the parsed ``argparse.Namespace`` and returns
an exit code.
"""
