# Copyright Statement

This document states, in plain language, the copyright posture of this
repository. For the legally binding terms, see `LICENSE`.

## Authorship

The original author of the work contained in this repository is the
git committer identified in the local repository configuration:

```
user.name  = benholl94-cmyk
user.email = 274793931+benholl94-cmyk@users.noreply.github.com
```

This is the canonical identity for copyright attribution purposes.
The numeric prefix `274793931` is the author's GitHub user ID and
serves as a stable, non-reassignable identifier independent of any
display-name changes.

## Scope of copyright

The copyright in this repository covers the original expression
authored by the committer. Specifically:

- **Source code** (workflows, scripts, schemas, configuration) — yes
- **Documentation** (`docs/`, `protocol/`, `README.md`, `CLAUDE.md`) — yes
- **Protocol specifications** in `protocol/` — yes (these are the
  author's original formalizations of the claims and reputation models)
- **Agent identity and capabilities** in `agent/` — yes
- **The selection, coordination, and arrangement** of the above — yes
  (compilation copyright under § 2 of the Berne Convention)

The copyright does NOT cover:

- **Ideas, methods, or systems** described in the documentation
  (copyright protects expression, not ideas — § 9(2) Berne)
- **Standards or specifications published elsewhere** that are merely
  referenced
- **Output generated by tools in `tools/` when run against external
  inputs** (only the tool itself is copyrighted; outputs belong to
  their respective input owners)

## Provenance

Every file in this repository carries, in its git history, a
cryptographic commit signature from the author's GPG/SSH key (if
configured) or the committer identity above. The agent protocol
defined in `protocol/CLAIMS.md` extends this to runtime output via
Ed25519 signatures bound to `agent/identity.json`.

To verify the chain of authorship at any point:

```bash
# Verify git authorship
git log --pretty=fuller

# Verify agent signature on a claim file
./tools/verify-claim.sh path/to/claim.json
```

## What this is NOT

This is **not** an attempt to claim exclusive ownership over:

- The general concept of issue tracking
- The general concept of AI agent coordination
- Any idea, pattern, or architecture that pre-existed this work
- Any implementation of similar ideas by other parties

The repository claims copyright only over the specific expression
realized in its files at their specific commit hashes. Independent
re-implementations by other parties remain theirs to copyright.
