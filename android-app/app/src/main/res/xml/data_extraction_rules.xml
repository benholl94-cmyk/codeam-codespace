<?xml version="1.0" encoding="utf-8"?>
<!--
    data_extraction_rules.xml — disable cloud backup and device-transfer
    of the app's private data. The owner's state (encrypted, owner-unlock-
    gated) must never leave this device via Google's auto-backup.
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>